require 'spec_helper'

describe Corydoras do
  it 'has a version number' do
    expect(Corydoras::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
